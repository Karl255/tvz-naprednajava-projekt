apiVersion: apps/v1
kind: Deployment
metadata:
  name: dijezet-frontend
  namespace: dijezetapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dijezet-frontend
  template:
    metadata:
      labels:
        app: dijezet-frontend
    spec:
      containers:
        - name: nginx
          image: ghcr.io/karl255/dijezet-frontend:4f3fb829823016597cd4d167e8ffda1a7f01e21f
          ports:
            - containerPort: 80
